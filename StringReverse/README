A short C# practice program that takes a string of characters, stores that string,
and allows the user to reverse that string or print those results to the console.
Nothing too fancy, but was a great opportunity to sharpen my C# skills.
